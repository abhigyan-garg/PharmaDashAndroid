<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.PharmaDash.PharmaDashApp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyB_bcy42zRFG3pWKFBdGBN-JM1aHo1h-dw" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.PharmaDash.PharmaDashApp.ReceiptScannerActivity"></activity>

        <service
            android:name="com.PharmaDash.PharmaDashApp.LocationUpdates"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="com.PharmaDash.PharmaDashApp.onAppKilled"
            android:stopWithTask="false" />

        <activity android:name="com.PharmaDash.PharmaDashApp.HistorySingleActivity" />
        <activity android:name="com.PharmaDash.PharmaDashApp.HistoryActivity" />
        <activity android:name="com.PharmaDash.PharmaDashApp.DriverSettingsActivity" />
        <activity android:name="com.PharmaDash.PharmaDashApp.CustomerSettingsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.PharmaDash.PharmaDashApp.CustomerMapActivity"
            android:label="@string/title_activity_customer_map" />
        <activity
            android:name="com.PharmaDash.PharmaDashApp.DriverMapActivity"
            android:label="@string/title_activity_driver_map" />
        <activity android:name="com.PharmaDash.PharmaDashApp.CustomerLoginActivity" />
        <activity android:name="com.PharmaDash.PharmaDashApp.DriverLoginActivity" />
        <activity android:name="com.PharmaDash.PharmaDashApp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.mydomain.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>